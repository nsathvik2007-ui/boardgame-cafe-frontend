# Copy to .env.production and set to the real backend URL before building for deploy.
# Falls back to http://cafe.local:8000 (local dev) if unset.
VITE_API_URL=https://your-backend-domain.example.com
